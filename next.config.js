/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Static HTML is served from /public
  // API routes are serverless functions under /api
};

module.exports = nextConfig;
